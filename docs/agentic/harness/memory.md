## Agent 记忆

这里有点头疼，目前好像没有SOTA的实现、现有方案也没有银弹。
对于 Coding Agent，大概率依赖于仓库内文档的维护。
对于 全体Agent, 我还是期望有一个通用的方案。
通用是指 人和Agent都能读写都能改。
所以有点rule out掉OpenViking之类的方案了，虽然我们能搭。
Outline 也许可以。之后看看。@TODO

印象里：
```
现有的一切 agent 记忆方案都没法在，做到很好的召回的情况下，聊天记录条数和涉及到的人数两个维度上很好地 scale，我永远不可能做一个只是看起来能用的方案，在有一个能说服我它的确「连续跑一年后仍然可行」的方案之前我看不到做这件事的价值
我对 Cahciua 这个项目的态度就是，可以缺功能，但每个做了的功能都必须是被证明它这个功能是完整且有道理的，必须是 SOTA 或者有 choose this over other 方案的理由
       ——— Menci, 2026/04/14, Project Memoh群里
```

不追求完美也不折腾，先让东西能跑起来再说。
对于本仓库，迁移之后没有恢复OpenSpec。感觉是需要恢复的。
