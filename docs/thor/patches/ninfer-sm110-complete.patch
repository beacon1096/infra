--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -1,14 +1,13 @@
 cmake_minimum_required(VERSION 3.28)
 
-# The current implementation is compiled only for sm_120a. Set and validate the
-# architecture before CUDA compiler detection so CMake never injects a generic
-# host-toolchain default or accepts an unsupported target.
+# Select the registered architecture before CUDA compiler detection to avoid
+# injecting a generic host-toolchain default.
 if(NOT DEFINED CMAKE_CUDA_ARCHITECTURES)
   set(CMAKE_CUDA_ARCHITECTURES 120a CACHE STRING "CUDA architectures to build")
 endif()
-if(NOT CMAKE_CUDA_ARCHITECTURES STREQUAL "120a")
+if(NOT (CMAKE_CUDA_ARCHITECTURES STREQUAL "120a" OR CMAKE_CUDA_ARCHITECTURES STREQUAL "110a"))
   message(FATAL_ERROR
-    "NInfer supports only CMAKE_CUDA_ARCHITECTURES=120a; got "
+    "NInfer supports only CMAKE_CUDA_ARCHITECTURES=120a or 110a; got "
     "'${CMAKE_CUDA_ARCHITECTURES}'")
 endif()
 
@@ -33,9 +32,9 @@
   set(CMAKE_JOB_POOL_LINK cuda_link)
 endif()
 
-if(CMAKE_CUDA_COMPILER_VERSION VERSION_LESS 13.1)
+if(CMAKE_CUDA_COMPILER_VERSION VERSION_LESS 13.0)
   message(FATAL_ERROR
-    "NInfer requires CUDA 13.1 or newer; found CUDA compiler "
+    "NInfer requires CUDA 13.0 or newer; found CUDA compiler "
     "${CMAKE_CUDA_COMPILER_VERSION}")
 endif()
 
--- a/docs/maintainer/model-weight-execution.md
+++ b/docs/maintainer/model-weight-execution.md
@@ -1121,17 +1121,12 @@
 调用时，相应存储原语/Op 报告无法处理该几何。错误不来自未登记的尺寸名称。加入缺失的真实
 实现后，同一架构/config/绑定机制可以使用它，不需要为这个训练实例复制模型算法。
 
-第二种输入使用已知 27B 几何和 NVFP4 gate/up，但 recipe 将其使用许可设为 `A16Only`。它具有
-合法 payload、scale 与绑定。现有
-[LinearSwiGLU 合同](../../include/ninfer/ops/linear_swiglu.h)的 NVFP4 A16Only 路径只覆盖
-`T<=16`，AllowA4 才覆盖更大 T。
-
-如果 Program 的资源准备已经向该 Op 查询包含 `T=32` 的范围，准备便自然失败；若此前只实际
-准备或执行了可处理的范围，触发不支持调用时失败。一次 `T=1` decode 成功不允许 Op 在之后
-的 prefill 中绕过 A16Only。错误传播也不需要 loader 事先扫描全部 T。
-
-这个例子明确了三条边界：配置一致性不等于执行实现存在，权重可解码不等于计算许可可满足，
-warmup 不是全部调用域的支持证明。失败仍保持资源与输出事务的既有语义。
+已登记的 27B NVFP4 gate/up 则说明另一种情况：合法 payload、scale 与绑定配合
+`A16Only`，现在可以覆盖所有正 T。[LinearSwiGLU 合同](../../include/ninfer/ops/linear_swiglu.h)
+允许按最多 16 token 的块执行融合 A16 内核，较大 prefill 无需绕过计算许可或改用 A4。
+
+配置一致性仍不等于其他几何的执行实现存在；一次 decode 成功也不能证明全部调用域。
+资源准备和运行调用必须保持相同计算许可，错误传播保持资源与输出事务的既有语义。
 
 ### 14.6 Attention 行序合法，不代表已有 Op 可以消费
 
--- a/docs/maintainer/qwen3.6-27b-artifact.md
+++ b/docs/maintainer/qwen3.6-27b-artifact.md
@@ -725,7 +725,8 @@
 output layer 4.
 
 All NVFP4 Text parents and BF16 exceptions described above are bound and executable. Every Text
-phase passes `AllowA4` for NVFP4 weights and `A16Only` for all other formats. Each semantic Op then
+phase passes `AllowA4` for NVFP4 weights when W4A4 is enabled in the build, otherwise `A16Only`;
+BF16 parents use `A16Only`. Each semantic Op then
 resolves its qualified route from the exact geometry and T; Prefill, ordinary decode, and
 speculative target verify do not create separate activation-policy variants. MTP and Vision use
 their registered storage and execution paths. With all startup features enabled, 1054 tensors and six
--- a/include/ninfer/ops/linear_swiglu.h
+++ b/include/ninfer/ops/linear_swiglu.h
@@ -25,7 +25,7 @@
                                                                  std::int32_t max_tokens);
 
 /**
- * Policy-bearing capacity query. Q4/W8 admit A16Only. NVFP4 admits A16Only through T=16 and
+ * Policy-bearing capacity query. Q4/W8 admit A16Only. NVFP4 admits A16Only and
  * AllowA4 for every positive T. Row-scaled FP8 admits A16Only and AllowA8 for every positive T.
  * A permissive policy covers whichever qualified route the private resolver selects across the
  * requested interval.
@@ -74,9 +74,7 @@
                    WorkspaceArena& ws, cudaStream_t stream);
 
 /**
- * A16-only convenience form. Q4/W8 and row-scaled FP8 retain their complete positive-T domain.
- * NVFP4 is admitted only through T=16; larger NVFP4 extents require the policy-bearing AllowA4
- * form.
+ * A16-only convenience form. All registered formats admit every positive T.
  */
 void linear_swiglu(const Tensor& x, const Weight& gate_up_weight, Tensor& out, WorkspaceArena& ws,
                    cudaStream_t stream);
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -17,6 +17,7 @@
 add_library(ninfer_core STATIC
   core/arena.cu
   core/device.cu
+  core/memory.cpp
   core/dtype.cpp
   core/host_worker_pool.cpp
   core/gdn_replay_records.cpp
@@ -46,13 +47,33 @@
 ninfer_internal_includes(ninfer_artifact)
 target_link_libraries(ninfer_artifact PUBLIC ninfer_core)
 
+# These W4A4 kernels have not been qualified for SM110. Keep its target policy
+# on the A16 implementation until that kernel port is validated.
+if(CMAKE_CUDA_ARCHITECTURES STREQUAL "120a")
+  set(NINFER_HAS_NVFP4_W4A4 1)
+  set(NINFER_NVFP4_W4A4_SOURCES
+    ops/linear/nvfp4/nvfp4_w4a4.cu
+    ops/attn_input_proj/nvfp4/nvfp4_attn_input_w4a4.cu
+    ops/gdn_input_proj/nvfp4/nvfp4_gdn_input_w4a4.cu
+    ops/linear_add/nvfp4/nvfp4_linear_add_w4a4.cu
+    ops/linear_swiglu/nvfp4/nvfp4_linear_swiglu_w4a4.cu)
+  set(NINFER_NVFP4_W4A4_TMA_SOURCES
+    ops/linear/nvfp4/nvfp4_w4a4_tma.cu
+    ops/linear_swiglu/nvfp4/nvfp4_linear_swiglu_w4a4_tma.cu)
+else()
+  set(NINFER_HAS_NVFP4_W4A4 0)
+  set(NINFER_NVFP4_W4A4_SOURCES ops/linear/nvfp4/nvfp4_w4a4_stub.cpp)
+  set(NINFER_NVFP4_W4A4_TMA_SOURCES)
+endif()
+target_compile_definitions(ninfer_core PUBLIC
+  NINFER_HAS_NVFP4_W4A4=${NINFER_HAS_NVFP4_W4A4})
+
 # Warp-specialized SM120 NVFP4 kernels use setmaxnreg to transfer registers from producer
 # warpgroups to consumer warpgroups. Relocatable device code makes ptxas discard that contract, so
 # these self-contained kernels are intentionally compiled in a non-RDC archive and called through
 # host launchers from ninfer_ops.
 add_library(ninfer_nvfp4_non_rdc STATIC
-  ops/linear/nvfp4/nvfp4_w4a4_tma.cu
-  ops/linear_swiglu/nvfp4/nvfp4_linear_swiglu_w4a4_tma.cu
+  ${NINFER_NVFP4_W4A4_TMA_SOURCES}
   ops/softmax_attention/dense/causal_cache/prompt_nvfp4_non_rdc.cu)
 ninfer_internal_includes(ninfer_nvfp4_non_rdc)
 set_target_properties(ninfer_nvfp4_non_rdc PROPERTIES
@@ -136,7 +157,7 @@
   ops/attn_input_proj/fp8/fp8_attn_input_plan.cpp
   ops/attn_input_proj/nvfp4/nvfp4_attn_input_decode.cu
   ops/attn_input_proj/nvfp4/nvfp4_attn_input_small_t.cu
-  ops/attn_input_proj/nvfp4/nvfp4_attn_input_w4a4.cu
+  ${NINFER_NVFP4_W4A4_SOURCES}
   ops/attn_input_proj/nvfp4/nvfp4_attn_input_plan.cpp
   ops/attn_input_proj/q4_q5/q4_q5_attn_input_gemm_mma.cu
   ops/attn_input_proj/q4_q5/q4_q5_attn_input_small_t.cu
@@ -155,7 +176,6 @@
   ops/gdn_input_proj/fp8/fp8_gdn_conv_plan.cpp
   ops/gdn_input_proj/nvfp4/nvfp4_gdn_input_decode.cu
   ops/gdn_input_proj/nvfp4/nvfp4_gdn_input_small_t.cu
-  ops/gdn_input_proj/nvfp4/nvfp4_gdn_input_w4a4.cu
   ops/gdn_input_proj/nvfp4/nvfp4_gdn_input_plan.cpp
   ops/gdn_input_proj/nvfp4/nvfp4_gdn_snapshot_decode.cu
   ops/gdn_input_proj/nvfp4/nvfp4_gdn_snapshot_small_t.cu
@@ -193,7 +213,6 @@
   ops/linear/nvfp4/nvfp4_format.cpp
   ops/linear/nvfp4/nvfp4_gemv.cu
   ops/linear/nvfp4/nvfp4_small_t.cu
-  ops/linear/nvfp4/nvfp4_w4a4.cu
   ops/linear/nvfp4/nvfp4_dispatch.cpp
   ops/linear/q4/q4_small_t_mma.cu
   ops/linear/q4/q4_rowsplit_gemm_mma.cu
@@ -210,7 +229,6 @@
   ops/linear_add/bf16/bf16_linear_add_plan.cpp
   ops/linear_add/nvfp4/nvfp4_linear_add_decode.cu
   ops/linear_add/nvfp4/nvfp4_linear_add_small_t.cu
-  ops/linear_add/nvfp4/nvfp4_linear_add_w4a4.cu
   ops/linear_add/nvfp4/nvfp4_linear_add_plan.cpp
   ops/linear_add/fp8/fp8_linear_add_decode.cu
   ops/linear_add/fp8/fp8_linear_add_small_t.cu
@@ -229,7 +247,6 @@
   ops/linear_swiglu/q4/q4_linear_swiglu_plan.cpp
   ops/linear_swiglu/nvfp4/nvfp4_linear_swiglu_decode.cu
   ops/linear_swiglu/nvfp4/nvfp4_linear_swiglu_small_t.cu
-  ops/linear_swiglu/nvfp4/nvfp4_linear_swiglu_w4a4.cu
   ops/linear_swiglu/nvfp4/nvfp4_linear_swiglu_plan.cpp
   ops/linear_swiglu/fp8/fp8_linear_swiglu_decode.cu
   ops/linear_swiglu/fp8/fp8_linear_swiglu_small_t.cu
--- a/src/core/device.cu
+++ b/src/core/device.cu
@@ -1,7 +1,10 @@
 #include "core/device.h"
-
+#include "core/memory.h"
+
+#include <algorithm>
 #include <cstdio>
 #include <cstdlib>
+#include <fstream>
 #include <stdexcept>
 #include <string>
 #include <utility>
@@ -128,6 +131,22 @@
 }
 
 std::size_t DeviceContext::total_vram() const noexcept { return props.totalGlobalMem; }
+
+std::size_t DeviceContext::available_memory() const {
+    bind_to_current_thread();
+    std::size_t free_bytes = 0;
+    std::size_t total_bytes = 0;
+    CUDA_CHECK(cudaMemGetInfo(&free_bytes, &total_bytes));
+#if defined(__linux__)
+    if (props.integrated) {
+        // CUDA free memory excludes reclaimable Linux page cache on integrated GPUs.
+        std::ifstream meminfo("/proc/meminfo");
+        return select_available_memory(true, free_bytes, std::min(total_bytes, total_vram()),
+                                       parse_mem_available(meminfo));
+    }
+#endif
+    return free_bytes;
+}
 
 void DeviceContext::synchronize() const { CUDA_CHECK(cudaStreamSynchronize(stream)); }
 
--- a/src/core/device.h
+++ b/src/core/device.h
@@ -38,6 +38,7 @@
     int multiprocessor_count() const noexcept;
     DeviceExecutionView execution_view() const noexcept;
     std::size_t total_vram() const noexcept;
+    std::size_t available_memory() const;
     void synchronize() const;
 };
 
--- /dev/null
+++ b/src/core/memory.cpp
@@ -0,0 +1,43 @@
+#include "core/memory.h"
+
+#include <algorithm>
+#include <charconv>
+#include <limits>
+#include <sstream>
+#include <string>
+
+namespace ninfer {
+
+std::optional<std::size_t> parse_mem_available(std::istream& input) {
+    std::optional<std::size_t> available;
+    std::string line;
+    while (std::getline(input, line)) {
+        std::istringstream fields(line);
+        std::string key;
+        if (!(fields >> key) || key != "MemAvailable:") { continue; }
+        std::string value;
+        std::string unit;
+        std::string extra;
+        if (available || !(fields >> value >> unit) || unit != "kB" || fields >> extra) {
+            return std::nullopt;
+        }
+        std::size_t kib = 0;
+        const auto result = std::from_chars(value.data(), value.data() + value.size(), kib);
+        if (result.ec != std::errc{} || result.ptr != value.data() + value.size() ||
+            kib > std::numeric_limits<std::size_t>::max() / 1024) {
+            return std::nullopt;
+        }
+        available = kib * 1024;
+    }
+    if (input.bad() || !input.eof()) { return std::nullopt; }
+    return available;
+}
+
+std::size_t select_available_memory(bool integrated, std::size_t cuda_free,
+                                    std::size_t device_total,
+                                    std::optional<std::size_t> host_available) noexcept {
+    if (integrated && host_available) { return std::min(*host_available, device_total); }
+    return cuda_free;
+}
+
+} // namespace ninfer
--- /dev/null
+++ b/src/core/memory.h
@@ -0,0 +1,14 @@
+#pragma once
+
+#include <cstddef>
+#include <istream>
+#include <optional>
+
+namespace ninfer {
+
+std::optional<std::size_t> parse_mem_available(std::istream& input);
+std::size_t select_available_memory(bool integrated, std::size_t cuda_free,
+                                    std::size_t device_total,
+                                    std::optional<std::size_t> host_available) noexcept;
+
+} // namespace ninfer
--- /dev/null
+++ b/src/ops/linear/nvfp4/nvfp4_w4a4_stub.cpp
@@ -0,0 +1,101 @@
+// Link-time stubs for the NVFP4 W4A4 launchers.
+//
+// This build has not enabled the W4A4 kernels. Target policies select A16;
+// explicit unsupported W4A4 calls fail here instead of implying hardware support.
+#include "ops/linear/nvfp4/nvfp4_w4a4_plan.h"
+#include "ops/linear/nvfp4/nvfp4_w4a4_tma_launch.h"
+#include "ops/attn_input_proj/nvfp4/nvfp4_attn_input_plan.h"
+#include "ops/gdn_input_proj/nvfp4/nvfp4_gdn_input_plan.h"
+#include "ops/linear_add/nvfp4/nvfp4_linear_add_plan.h"
+#include "ops/linear_swiglu/nvfp4/nvfp4_linear_swiglu_plan.h"
+#include "ops/linear_swiglu/nvfp4/nvfp4_linear_swiglu_w4a4_tma_launch.h"
+
+#include <cuda_runtime.h>
+
+#include <cstdint>
+#include <stdexcept>
+
+namespace {
+
+[[noreturn]] void throw_unavailable() {
+    throw std::runtime_error(
+        "nvfp4 W4A4 kernels are not enabled in this build; use the A16 route instead");
+}
+
+} // namespace
+
+namespace ninfer::ops::detail {
+
+void launch_nvfp4_w4a4_quantize(const Tensor& x, const Weight& weight,
+                                Nvfp4W4a4Workspace workspace, cudaStream_t stream) {
+    throw_unavailable();
+}
+
+void launch_nvfp4_w4a4(const Tensor& x, const Weight& weight, Tensor& out,
+                       Nvfp4W4a4Workspace workspace, cudaStream_t stream) {
+    throw_unavailable();
+}
+
+void launch_nvfp4_w4a4_tma_linear(Nvfp4Problem problem, const std::uint8_t* activation_codes,
+                                  const std::uint8_t* activation_scales,
+                                  const std::uint8_t* weight_codes,
+                                  const std::uint8_t* weight_scales, __nv_bfloat16* output,
+                                  std::int32_t tokens, float alpha, cudaStream_t stream) {
+    throw_unavailable();
+}
+
+void launch_nvfp4_w4a4_tma_attention(const std::uint8_t* activation_codes,
+                                     const std::uint8_t* activation_scales,
+                                     const std::uint8_t* weight_codes,
+                                     const std::uint8_t* weight_scales, __nv_bfloat16* query,
+                                     __nv_bfloat16* gate, __nv_bfloat16* key, __nv_bfloat16* value,
+                                     std::int32_t tokens, float alpha, cudaStream_t stream) {
+    throw_unavailable();
+}
+
+void launch_nvfp4_w4a4_tma_gdn(const std::uint8_t* activation_codes,
+                               const std::uint8_t* activation_scales,
+                               const std::uint8_t* weight_codes, const std::uint8_t* weight_scales,
+                               __nv_bfloat16* qkv, __nv_bfloat16* z, std::int32_t tokens,
+                               float alpha, cudaStream_t stream) {
+    throw_unavailable();
+}
+
+void launch_nvfp4_w4a4_tma_linear_add(Nvfp4Problem problem, const std::uint8_t* activation_codes,
+                                      const std::uint8_t* activation_scales,
+                                      const std::uint8_t* weight_codes,
+                                      const std::uint8_t* weight_scales, __nv_bfloat16* residual,
+                                      std::int32_t tokens, float alpha, cudaStream_t stream) {
+    throw_unavailable();
+}
+
+void nvfp4_attn_input_w4a4_launch(const Tensor& x, const Weight& weight, Tensor& q, Tensor& gate,
+                                  Tensor& k, Tensor& v, Nvfp4W4a4Workspace workspace,
+                                  cudaStream_t stream) {
+    throw_unavailable();
+}
+
+void nvfp4_gdn_input_w4a4_launch(const Tensor& x, const Weight& weight, Tensor& qkv, Tensor& z,
+                                 Nvfp4W4a4Workspace workspace, cudaStream_t stream) {
+    throw_unavailable();
+}
+
+void nvfp4_linear_add_w4a4_launch(const Tensor& x, const Weight& weight, Tensor& residual,
+                                  Nvfp4W4a4Workspace workspace, cudaStream_t stream) {
+    throw_unavailable();
+}
+
+void nvfp4_linear_swiglu_w4a4_launch(const Tensor& x, const Weight& weight, Tensor& out,
+                                     WorkspaceArena& workspace, cudaStream_t stream) {
+    throw_unavailable();
+}
+
+void launch_nvfp4_linear_swiglu_w4a4_tma(const std::uint8_t* activation_codes,
+                                         const std::uint8_t* activation_scales,
+                                         const std::uint8_t* weight_codes,
+                                         const std::uint8_t* weight_scales, __nv_bfloat16* output,
+                                         std::int32_t tokens, float alpha, cudaStream_t stream) {
+    throw_unavailable();
+}
+
+} // namespace ninfer::ops::detail
--- a/src/ops/linear_swiglu/nvfp4/nvfp4_linear_swiglu_plan.cpp
+++ b/src/ops/linear_swiglu/nvfp4/nvfp4_linear_swiglu_plan.cpp
@@ -8,6 +8,7 @@
 
 #include <algorithm>
 #include <cstddef>
+#include <cstdint>
 #include <stdexcept>
 
 namespace ninfer::ops::detail {
@@ -15,7 +16,7 @@
 
 enum class Nvfp4LinearSwiGluRoute {
     DecodeFusedA16,
-    SmallTFusedA16,
+    ChunkedFusedA16,
     FusedW4A4,
     LinearW4A4Post,
     TmaFusedW4A4,
@@ -31,16 +32,34 @@
     }
     if (policy == LinearPolicy::A16Only) {
         if (tokens == 1) { return Nvfp4LinearSwiGluRoute::DecodeFusedA16; }
-        if (tokens <= 16) { return Nvfp4LinearSwiGluRoute::SmallTFusedA16; }
-        throw std::invalid_argument("nvfp4 linear_swiglu A16 is registered only through T=16");
+        return Nvfp4LinearSwiGluRoute::ChunkedFusedA16;
     }
     if (tokens == 1) { return Nvfp4LinearSwiGluRoute::DecodeFusedA16; }
-    if (tokens <= 4) { return Nvfp4LinearSwiGluRoute::SmallTFusedA16; }
+    if (tokens <= 4) { return Nvfp4LinearSwiGluRoute::ChunkedFusedA16; }
     if (tokens <= kFusedMaxTokens) { return Nvfp4LinearSwiGluRoute::FusedW4A4; }
     if (tokens >= kTmaBlockM && (tokens % kTmaBlockM) == 0) {
         return Nvfp4LinearSwiGluRoute::TmaFusedW4A4;
     }
     return Nvfp4LinearSwiGluRoute::LinearW4A4Post;
+}
+
+void launch_a16_chunks(const Tensor& x, const Weight& weight, Tensor& out,
+                       cudaStream_t stream) {
+    constexpr std::int32_t kOutputRows = Nvfp4MlpGateUpGeometry::kOutputRows / 2;
+    for (std::int32_t begin = 0; begin < x.ne[1]; begin += kNvfp4SwiGluLastSmallT) {
+        const std::int32_t active = std::min(kNvfp4SwiGluLastSmallT, x.ne[1] - begin);
+        auto* input = static_cast<std::uint16_t*>(x.data) +
+                      static_cast<std::int64_t>(begin) * weight.k;
+        auto* output = static_cast<std::uint16_t*>(out.data) +
+                       static_cast<std::int64_t>(begin) * kOutputRows;
+        Tensor input_chunk(input, DType::BF16, {weight.k, active});
+        Tensor output_chunk(output, DType::BF16, {kOutputRows, active});
+        if (active == 1) {
+            nvfp4_linear_swiglu_decode_launch(input_chunk, weight, output_chunk, stream);
+        } else {
+            nvfp4_linear_swiglu_small_t_launch(input_chunk, weight, output_chunk, stream);
+        }
+    }
 }
 
 struct Nvfp4LinearSwiGluWorkspace {
@@ -117,8 +136,8 @@
     case Nvfp4LinearSwiGluRoute::DecodeFusedA16:
         nvfp4_linear_swiglu_decode_launch(x, weight, out, stream);
         return;
-    case Nvfp4LinearSwiGluRoute::SmallTFusedA16:
-        nvfp4_linear_swiglu_small_t_launch(x, weight, out, stream);
+    case Nvfp4LinearSwiGluRoute::ChunkedFusedA16:
+        launch_a16_chunks(x, weight, out, stream);
         return;
     case Nvfp4LinearSwiGluRoute::FusedW4A4:
         nvfp4_linear_swiglu_w4a4_launch(x, weight, out, workspace, stream);
--- a/src/ops/linear_swiglu/nvfp4/nvfp4_linear_swiglu_plan.h
+++ b/src/ops/linear_swiglu/nvfp4/nvfp4_linear_swiglu_plan.h
@@ -10,6 +10,8 @@
 #include <cstdint>
 
 namespace ninfer::ops::detail {
+
+inline constexpr std::int32_t kNvfp4SwiGluLastSmallT = 16;
 
 [[nodiscard]] std::size_t nvfp4_linear_swiglu_workspace_capacity_bytes(LinearPolicy policy,
                                                                        std::int32_t min_tokens,
--- a/src/ops/linear_swiglu/nvfp4/nvfp4_linear_swiglu_small_t.cu
+++ b/src/ops/linear_swiglu/nvfp4/nvfp4_linear_swiglu_small_t.cu
@@ -101,14 +101,15 @@
         &launch_exact<kNvfp4FirstSmallT + static_cast<int>(Offsets)>...};
 }
 
-constexpr auto kLaunchers = make_launchers(std::make_index_sequence<16 - kNvfp4FirstSmallT + 1>{});
+constexpr auto kLaunchers = make_launchers(
+    std::make_index_sequence<kNvfp4SwiGluLastSmallT - kNvfp4FirstSmallT + 1>{});
 
 } // namespace
 
 void nvfp4_linear_swiglu_small_t_launch(const Tensor& x, const Weight& weight, Tensor& out,
                                         cudaStream_t stream) {
     const std::size_t index = static_cast<std::size_t>(x.ne[1] - kNvfp4FirstSmallT);
-    kLaunchers[index](x, weight, out, stream);
+    kLaunchers.at(index)(x, weight, out, stream);
 }
 
 } // namespace ninfer::ops::detail
--- a/src/targets/qwen3_6/impl/runtime/layouts_impl.h
+++ b/src/targets/qwen3_6/impl/runtime/layouts_impl.h
@@ -701,8 +701,9 @@
         }
         break;
     }
-    if (device.compute_capability() != 120) {
-        throw std::invalid_argument("Qwen3.6 family runtime requires compute capability 12.0");
+    if (device.compute_capability() != 120 && device.compute_capability() != 110) {
+        throw std::invalid_argument(
+            "Qwen3.6 family runtime requires compute capability 12.0 or 11.0");
     }
 }
 
--- a/src/targets/qwen3_6_27b/impl/variant.cpp
+++ b/src/targets/qwen3_6_27b/impl/variant.cpp
@@ -43,7 +43,8 @@
     }
 }
 
-constexpr ops::LinearPolicy kNvfp4TextPolicy = ops::LinearPolicy::AllowA4;
+constexpr ops::LinearPolicy kNvfp4TextPolicy =
+    NINFER_HAS_NVFP4_W4A4 ? ops::LinearPolicy::AllowA4 : ops::LinearPolicy::A16Only;
 constexpr ops::LinearPolicy kFp8TextPolicy   = ops::LinearPolicy::AllowA8;
 
 ops::LinearPolicy text_policy(const Weight& weight) {
--- a/src/targets/registry.cpp
+++ b/src/targets/registry.cpp
@@ -64,10 +64,9 @@
     }
 }
 
-std::size_t runtime_bytes_after_planned_weights(std::uint64_t weight_bytes) {
-    std::size_t free_bytes  = 0;
-    std::size_t total_bytes = 0;
-    CUDA_CHECK(cudaMemGetInfo(&free_bytes, &total_bytes));
+std::size_t runtime_bytes_after_planned_weights(const DeviceContext& device,
+                                               std::uint64_t weight_bytes) {
+    const std::size_t free_bytes = device.available_memory();
     if (weight_bytes > free_bytes) {
         throw std::invalid_argument("model weights require " + std::to_string(weight_bytes) +
                                     " bytes of device memory, but only " +
@@ -75,13 +74,6 @@
                                     " bytes are free before loading weights");
     }
     return free_bytes - static_cast<std::size_t>(weight_bytes);
-}
-
-std::size_t current_free_device_bytes() {
-    std::size_t free_bytes  = 0;
-    std::size_t total_bytes = 0;
-    CUDA_CHECK(cudaMemGetInfo(&free_bytes, &total_bytes));
-    return free_bytes;
 }
 
 template <class Target, class Loaded, class Instance>
@@ -106,7 +98,7 @@
     auto sequence_planner = Target::make_sequence_planner(device, options, weights_profile);
     const runtime::SequenceCapacityCurve curve = sequence_planner.capacity_curve();
     const std::size_t preflight_runtime_bytes =
-        runtime_bytes_after_planned_weights(load_plan.materialization().device_capacity_bytes);
+        runtime_bytes_after_planned_weights(device, load_plan.materialization().device_capacity_bytes);
     (void)runtime::resolve_kv_capacity(options.kv_capacity, curve, preflight_runtime_bytes);
     target_plan_phase.complete();
 
@@ -118,7 +110,7 @@
     auto model = Target::construct_loaded_model(std::move(load_plan), std::move(materialized));
     device.synchronize();
     runtime::KvCapacityResolution capacity_resolution =
-        runtime::resolve_kv_capacity(options.kv_capacity, curve, current_free_device_bytes());
+        runtime::resolve_kv_capacity(options.kv_capacity, curve, device.available_memory());
     auto sequence_plan = std::move(sequence_planner).finalize(capacity_resolution.main_page_groups);
     if (sequence_plan.device_reservation_bytes() != capacity_resolution.runtime_reservation_bytes ||
         sequence_plan.kv_capacity() != capacity_resolution.resolved_tokens) {
@@ -136,7 +128,7 @@
                                    device, options.startup_observer);
     device.synchronize();
     program_phase.complete();
-    instance->kv_capacity_resolution.available_after_startup_bytes = current_free_device_bytes();
+    instance->kv_capacity_resolution.available_after_startup_bytes = device.available_memory();
 
     LoadSummary summary;
     summary.target               = std::string(target_key);
--- a/tests/CMakeLists.txt
+++ b/tests/CMakeLists.txt
@@ -68,6 +68,7 @@
   SOURCES test_pretty_logging.cpp
   LIBRARIES ninfer_product_logging)
 ninfer_add_test(ninfer_device_test       SOURCES test_device.cpp)
+ninfer_add_test(ninfer_memory_test       SOURCES test_memory.cpp)
 ninfer_add_test(ninfer_decode_graph_test SOURCES test_decode_graph.cpp)
 ninfer_add_test(ninfer_tensor_test       SOURCES test_tensor.cpp)
 ninfer_add_test(ninfer_arena_test        SOURCES test_arena.cpp)
--- a/tests/ops/linear_add/test_nvfp4.cpp
+++ b/tests/ops/linear_add/test_nvfp4.cpp
@@ -89,19 +89,22 @@
 
 int run_shape(std::int32_t n, std::int32_t k, std::uint32_t seed) {
     const std::int32_t first_a4 = k == 6144 ? 7 : 8;
+    constexpr auto qualified_policy = NINFER_HAS_NVFP4_W4A4
+                                          ? ops::LinearPolicy::AllowA4
+                                          : ops::LinearPolicy::A16Only;
     const std::array invocations{
         Invocation{1, ops::LinearPolicy::A16Only},
         Invocation{4, ops::LinearPolicy::A16Only},
-        Invocation{first_a4, ops::LinearPolicy::AllowA4},
-        Invocation{17, ops::LinearPolicy::AllowA4},
-        Invocation{1024, ops::LinearPolicy::AllowA4},
-        Invocation{8, ops::LinearPolicy::AllowA4},
-        Invocation{16, ops::LinearPolicy::AllowA4},
-        Invocation{32, ops::LinearPolicy::AllowA4},
-        Invocation{64, ops::LinearPolicy::AllowA4},
-        Invocation{96, ops::LinearPolicy::AllowA4},
-        Invocation{128, ops::LinearPolicy::AllowA4},
-        Invocation{129, ops::LinearPolicy::AllowA4},
+        Invocation{first_a4, qualified_policy},
+        Invocation{17, qualified_policy},
+        Invocation{1024, qualified_policy},
+        Invocation{8, qualified_policy},
+        Invocation{16, qualified_policy},
+        Invocation{32, qualified_policy},
+        Invocation{64, qualified_policy},
+        Invocation{96, qualified_policy},
+        Invocation{128, qualified_policy},
+        Invocation{129, qualified_policy},
     };
     constexpr std::int32_t kMaximumTokens = 1024;
     quantized_weight::PatternedWeightOptions options;
--- a/tests/ops/linear_swiglu/test_nvfp4.cpp
+++ b/tests/ops/linear_swiglu/test_nvfp4.cpp
@@ -9,15 +9,19 @@
     using namespace ninfer::test::linear_swiglu;
 
     try {
-        constexpr std::array<std::int32_t, 4> kA16Cases{1, 4, 8, 16};
-        constexpr std::array<std::int32_t, 14> kA4Cases{2, 4, 5, 16, 56, 64, 65, 96, 97, 112, 128, 129, 256, 1024};
+        constexpr std::array<std::int32_t, 10> kA16Cases{1, 4, 8, 16, 17, 31, 32, 33, 256, 257};
         int failures = 0;
         failures += run_profile("LinearSwiGLU NVFP4_A16",
                                 {QType::NVFP4, 34816, 5120, 17408, 1801U, ActivationCompute::A16},
-                                kA16Cases);
-        failures +=
-            run_profile("LinearSwiGLU NVFP4_A4",
-                        {QType::NVFP4, 34816, 5120, 17408, 1803U, ActivationCompute::A4}, kA4Cases, std::array<std::int32_t, 4>{65, 97, 128, 129});
+                                kA16Cases, std::array<std::int32_t, 4>{17, 32, 33, 257});
+        if constexpr (NINFER_HAS_NVFP4_W4A4) {
+            constexpr std::array<std::int32_t, 14> kA4Cases{
+                2, 4, 5, 16, 56, 64, 65, 96, 97, 112, 128, 129, 256, 1024};
+            failures += run_profile(
+                "LinearSwiGLU NVFP4_A4",
+                {QType::NVFP4, 34816, 5120, 17408, 1803U, ActivationCompute::A4},
+                kA4Cases, std::array<std::int32_t, 4>{65, 97, 128, 129});
+        }
         std::cout << (failures == 0 ? "OK" : "FAIL") << " LinearSwiGLU NVFP4 correctness\n";
         return failures == 0 ? 0 : 1;
     } catch (const std::exception& error) {
--- /dev/null
+++ b/tests/test_memory.cpp
@@ -0,0 +1,66 @@
+#include "core/memory.h"
+
+#include <iostream>
+#include <limits>
+#include <sstream>
+#include <stdexcept>
+#include <string>
+
+namespace {
+
+void require(bool value, const char* message) {
+    if (!value) { throw std::runtime_error(message); }
+}
+
+std::optional<std::size_t> parse(const std::string& text) {
+    std::istringstream input(text);
+    return ninfer::parse_mem_available(input);
+}
+
+} // namespace
+
+int main() {
+    try {
+        using ninfer::select_available_memory;
+        require(parse("MemTotal: 128000000 kB\nMemFree: 100 kB\n"
+                      "MemAvailable:   59402308 kB\nCached: 123 kB\n") ==
+                    std::size_t{59402308} * 1024,
+                "Linux available memory was not parsed in KiB");
+        require(parse("MemAvailable:\t0 kB") == 0, "zero available memory was discarded");
+        for (const std::string text : {
+                 "MemFree: 100 kB\n", "MemAvailable: -1 kB\n",
+                 "MemAvailable: +1 kB\n", "MemAvailable: 1 MB\n",
+                 "MemAvailable: 1x kB\n", "MemAvailable: 1\n",
+                 "MemAvailable: 1 kB extra\n",
+                 "MemAvailable: 1 kB\nMemAvailable: 2 kB\n"}) {
+            require(!parse(text), "malformed or missing available memory was accepted");
+        }
+        require(!parse("MemAvailable: " +
+                       std::to_string(std::numeric_limits<std::size_t>::max()) + " kB"),
+                "KiB multiplication overflow was accepted");
+        require(!parse("MemAvailable: 99999999999999999999999999 kB"),
+                "integer parsing overflow was accepted");
+        std::istringstream unreadable("MemAvailable: 123 kB");
+        unreadable.setstate(std::ios::badbit);
+        require(!ninfer::parse_mem_available(unreadable), "read failure was accepted");
+        std::istringstream unopened;
+        unopened.setstate(std::ios::failbit);
+        require(!ninfer::parse_mem_available(unopened), "open failure was accepted");
+        require(select_available_memory(true, 11, 128, 56) == 56,
+                "integrated GPU did not include reclaimable memory");
+        require(select_available_memory(true, 11, 128, 4) == 4,
+                "host memory pressure was ignored");
+        require(select_available_memory(true, 11, 128, 200) == 128,
+                "host estimate exceeded device capacity");
+        require(select_available_memory(true, 11, 128, 0) == 0,
+                "zero host availability incorrectly fell back to CUDA");
+        require(select_available_memory(true, 11, 128, std::nullopt) == 11,
+                "missing host estimate did not fall back to CUDA");
+        require(select_available_memory(false, 11, 128, 56) == 11,
+                "discrete GPU incorrectly used host memory");
+    } catch (const std::exception& error) {
+        std::cerr << error.what() << '\n';
+        return 1;
+    }
+    return 0;
+}
